
model Transcription {
  id String @id @default(cuid())
  filename String
  voice Bytes
  text String?
  userId String
  createdAt              DateTime            @default(now())
  updatedAt              DateTime            @updatedAt
}
