
model VoiceRecord {
  id String @id @default(cuid())
  filename String
  voice BYTEA
  text String?
  createdAt              DateTime            @default(now())
  updatedAt              DateTime            @updatedAt
}
